---
phase: 23-websocket-server
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - src/worker/mod.rs
  - src/config/app.rs
autonomous: true
requirements: [WS-01, WS-02, WS-03]
must_haves:
  truths:
    - "axum ws feature compiles and axum::extract::ws module resolves"
    - "WsChannels type exists with std::sync::mpsc channel types"
    - "HandlerTask has pub ws: Option<WsChannels> field"
    - "AppLimits has max_ws_connections and ws_idle_timeout_ms fields"
    - "Existing configs without WS fields deserialize without error"
  artifacts:
    - path: "Cargo.toml"
      provides: "axum ws feature enabled"
      contains: 'features = [\"ws\"]'
    - path: "src/worker/mod.rs"
      provides: "WsChannels struct and HandlerTask ws field"
      contains: "pub ws: Option<WsChannels>"
    - path: "src/config/app.rs"
      provides: "WS config fields on AppLimits"
      contains: "max_ws_connections"
  key_links:
    - from: "src/worker/mod.rs"
      to: "tungstenite::Message"
      via: "WsChannels inbound_rx and outbound_tx types"
      pattern: "tungstenite::Message"
---

<objective>
Enable the axum WebSocket feature flag and define the foundational data types for WebSocket support: WsChannels (async-to-sync bridge), HandlerTask extension, and AppLimits WS configuration fields.

Purpose: This is the Wave 0 blocker. No WebSocket code compiles without the axum `ws` feature. The data types defined here are consumed by every subsequent plan.
Output: Compilable project with WS types and config fields; zero behavior change.
</objective>

<execution_context>
@/Users/gleicon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gleicon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-websocket-server/23-CONTEXT.md
@.planning/phases/23-websocket-server/23-RESEARCH.md
@.planning/phases/23-websocket-server/23-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/worker/mod.rs (lines 188-210):
```rust
#[derive(Debug)]
pub struct HandlerTask {
    pub entrypoint: String,
    pub request: NanoRequest,
    pub response_tx: oneshot::Sender<anyhow::Result<NanoResponse>>,
    pub hostname: String,
    pub start_time: std::time::Instant,
    pub cpu_time_limit_ms: u32,
    pub request_id: String,
    pub memory_limit_mb: u32,
}
unsafe impl Send for HandlerTask {}
```

From src/config/app.rs (lines 36-58):
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(deny_unknown_fields)]
pub struct AppLimits {
    #[serde(default = "default_memory_mb")]
    pub memory_mb: u32,
    #[serde(default = "default_timeout_secs")]
    pub timeout_secs: u32,
    #[serde(default = "default_workers")]
    pub workers: u32,
    #[serde(default = "default_cpu_time_ms")]
    pub cpu_time_ms: u32,
    #[serde(default = "default_cpu_time_enabled")]
    pub cpu_time_enabled: bool,
}
```

From Cargo.toml (line 39):
```toml
axum = "0.8"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable axum ws feature and add WsChannels + HandlerTask extension</name>
  <files>Cargo.toml, src/worker/mod.rs</files>
  <read_first>
    - Cargo.toml (line 39 — current axum declaration)
    - src/worker/mod.rs (lines 180-210 — HandlerTask struct and imports)
  </read_first>
  <action>
    1. In Cargo.toml line 39, change `axum = "0.8"` to `axum = { version = "0.8", features = ["ws"] }`. This is the Wave 0 blocker per D-01b confirmed in RESEARCH.md.

    2. In src/worker/mod.rs, add `use tungstenite;` to the imports near line 182 (after the existing `use tokio::sync::oneshot;`). The tungstenite crate is a transitive dependency of tokio-tungstenite (already in Cargo.toml line 83) and re-exported.

    3. Define the WsChannels struct ABOVE the HandlerTask struct (around line 184). Per D-06b and D-07b (locked decisions): WsChannels uses std::sync::mpsc (NOT tokio::sync::mpsc) because recv_timeout() is needed for idle shrink on the worker side.

    ```
    WsChannels struct fields:
      pub inbound_rx: std::sync::mpsc::Receiver<tungstenite::Message>
      pub outbound_tx: std::sync::mpsc::SyncSender<tungstenite::Message>
    ```

    Add a Debug impl for WsChannels (manual, since mpsc::Receiver does not derive Debug). The Debug impl should print "WsChannels { .. }" without field details.

    4. Add `pub ws: Option<WsChannels>` as the LAST field on HandlerTask. Add a doc comment: "WebSocket channels — Some means WS upgrade mode, None means normal HTTP".

    5. Update the HandlerTask::new() constructor (if one exists) to accept ws: Option<WsChannels> and pass it through. If no constructor exists, skip this — callers construct the struct directly.

    6. Update ALL call sites that construct HandlerTask to include `ws: None`. Search for `HandlerTask {` across the codebase. Expected call sites: src/http/router.rs (dispatch_to_worker_pool), src/worker/queue.rs (if it exists), any test files. Every site must add `ws: None` to avoid compile errors.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/rust/nano-rs && cargo check --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml line 39 reads: axum = { version = "0.8", features = ["ws"] }
    - src/worker/mod.rs contains `pub struct WsChannels` with inbound_rx: std::sync::mpsc::Receiver<tungstenite::Message> and outbound_tx: std::sync::mpsc::SyncSender<tungstenite::Message>
    - HandlerTask contains field `pub ws: Option<WsChannels>`
    - `cargo check --lib` succeeds with 0 errors
    - grep -r "HandlerTask {" src/ finds zero sites missing `ws:` field
  </acceptance_criteria>
  <done>axum ws feature enabled, WsChannels type defined, HandlerTask extended, all call sites updated, cargo check passes</done>
</task>

<task type="auto">
  <name>Task 2: Add WS configuration fields to AppLimits</name>
  <files>src/config/app.rs</files>
  <read_first>
    - src/config/app.rs (full file — need AppLimits struct, Default impl, and serde deny_unknown_fields attribute)
  </read_first>
  <action>
    1. Add two fields to the AppLimits struct (per locked decisions D-01b for max_ws_connections default and D-03b for ws_idle_timeout_ms). IMPORTANT: AppLimits has #[serde(deny_unknown_fields)] so these MUST go directly on the struct.

    Add after the cpu_time_enabled field:
    - `#[serde(default)] pub max_ws_connections: Option<u32>` with doc comment: "Maximum concurrent WebSocket connections per tenant. Default at runtime: floor(workers / 2). None means use default."
    - `#[serde(default)] pub ws_idle_timeout_ms: Option<u64>` with doc comment: "Idle timeout for WS worker threads in ms before shrink-to-zero. Default: 30000."

    Using Option<T> with #[serde(default)] means existing JSON configs without these fields will deserialize to None, which the runtime interprets as "use default". This preserves backward compatibility.

    2. Update the Default impl for AppLimits to set both new fields to None.

    3. Add two helper methods on AppLimits:
    - `pub fn effective_max_ws_connections(&self) -> u32` — returns self.max_ws_connections.unwrap_or(self.workers / 2). This implements D-01b: floor(worker_count / 2).
    - `pub fn effective_ws_idle_timeout_ms(&self) -> u64` — returns self.ws_idle_timeout_ms.unwrap_or(30_000). This implements D-03b.

    4. Add a unit test `test_app_limits_ws_defaults` in the existing #[cfg(test)] mod at the bottom of app.rs (or create one if absent). The test should verify:
    - Default AppLimits has max_ws_connections = None
    - effective_max_ws_connections() with workers=4 returns 2
    - effective_ws_idle_timeout_ms() returns 30000
    - JSON without WS fields deserializes successfully (backward compat with deny_unknown_fields)
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/rust/nano-rs && cargo test --lib -- app_limits_ws 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - AppLimits struct has fields max_ws_connections: Option<u32> and ws_idle_timeout_ms: Option<u64>
    - Both fields use #[serde(default)] attribute
    - Default impl sets both to None
    - effective_max_ws_connections() returns workers/2 when None
    - effective_ws_idle_timeout_ms() returns 30_000 when None
    - JSON `{"memory_mb":128,"timeout_secs":30,"workers":4,"cpu_time_ms":50,"cpu_time_enabled":true}` deserializes without error (deny_unknown_fields compat)
    - cargo test --lib passes
  </acceptance_criteria>
  <done>AppLimits has WS config fields with serde defaults, helper methods compute runtime defaults per D-01b and D-03b, backward-compatible deserialization verified by test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Data types only — no I/O, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | Cargo.toml | accept | Feature flag addition is a build-time change; no runtime risk |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new crates added — only enabling a feature on existing axum 0.8 |
</threat_model>

<verification>
1. `cargo check --lib` passes with 0 errors
2. `cargo test --lib` passes (all existing tests + new AppLimits WS test)
3. `grep -c "pub ws: Option<WsChannels>" src/worker/mod.rs` returns 1
4. `grep -c "max_ws_connections" src/config/app.rs` returns at least 2 (field + helper)
</verification>

<success_criteria>
- axum ws feature enabled and compilable
- WsChannels struct defined with std::sync::mpsc types
- HandlerTask extended with ws: Option<WsChannels>
- All existing HandlerTask construction sites compile with ws: None
- AppLimits has WS config fields with backward-compatible serde defaults
- All existing tests continue passing
</success_criteria>

<output>
Create `.planning/phases/23-websocket-server/23-01-SUMMARY.md` when done
</output>
