---
phase: 23-websocket-server
plan: 02
type: execute
wave: 1
depends_on: [23-01]
files_modified:
  - src/worker/tenant_pool.rs
autonomous: true
requirements: [WS-02]
must_haves:
  truths:
    - "TenantPool has ws_busy AtomicUsize field tracking active WS connections"
    - "TenantPool has max_ws_connections and ws_idle_timeout_ms fields populated from AppLimits"
    - "dispatch_ws method claims semaphore, finds idle worker or spawns new, sends HandlerTask"
    - "WS workers shrink to zero after ws_idle_timeout_ms of inactivity"
    - "TenantPool Drop joins WS worker threads"
  artifacts:
    - path: "src/worker/tenant_pool.rs"
      provides: "TenantPool WS pool fields and dispatch_ws method"
      contains: "ws_busy"
    - path: "src/worker/tenant_pool.rs"
      provides: "WsWorkerHandle struct and lazy spawn logic"
      contains: "WsWorkerHandle"
  key_links:
    - from: "src/worker/tenant_pool.rs"
      to: "src/worker/mod.rs"
      via: "WsChannels and HandlerTask types"
      pattern: "HandlerTask"
    - from: "src/worker/tenant_pool.rs"
      to: "src/config/app.rs"
      via: "AppLimits effective_max_ws_connections and effective_ws_idle_timeout_ms"
      pattern: "effective_max_ws_connections"
---

<objective>
Extend TenantPool with a lazy WebSocket worker pool: ws_busy counter, ws_workers Vec, dispatch_ws() method, and idle shrink logic. This is the server-side connection management layer per D-01 (pin-a-worker) and D-02b (WS pool as field on TenantPool).

Purpose: Workers that receive WebSocket tasks need to be tracked and lifecycle-managed separately from HTTP workers. The lazy pool spawns on demand and shrinks to zero after idle timeout per D-03b/D-11b.
Output: TenantPool can accept WS dispatch requests, enforce per-tenant connection limits (D-07), and manage WS worker thread lifecycle.
</objective>

<execution_context>
@/Users/gleicon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gleicon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-websocket-server/23-CONTEXT.md
@.planning/phases/23-websocket-server/23-RESEARCH.md
@.planning/phases/23-websocket-server/23-PATTERNS.md
@.planning/phases/23-websocket-server/23-01-PLAN.md

<interfaces>
<!-- From Plan 01 (Wave 0) outputs — executor MUST verify these exist before starting -->

From src/worker/mod.rs (added by Plan 01):
```rust
pub struct WsChannels {
    pub inbound_rx: std::sync::mpsc::Receiver<tungstenite::Message>,
    pub outbound_tx: std::sync::mpsc::SyncSender<tungstenite::Message>,
}

pub struct HandlerTask {
    // ... existing fields ...
    pub ws: Option<WsChannels>,  // NEW in Plan 01
}
```

From src/config/app.rs (added by Plan 01):
```rust
impl AppLimits {
    pub fn effective_max_ws_connections(&self) -> u32;  // floor(workers / 2)
    pub fn effective_ws_idle_timeout_ms(&self) -> u64;  // 30_000 default
}
```

From src/worker/tenant_pool.rs (existing):
```rust
pub struct TenantPool {
    hostname: String,
    workers: Vec<TenantWorker>,
    next_worker: AtomicU64,
    vfs_backend: VfsBackendEnum,
    control_plane: Option<ControlPlane>,
}

struct TenantWorker {
    task_tx: mpsc::Sender<HandlerTask>,
    thread: Option<thread::JoinHandle<()>>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WS pool fields to TenantPool and WsWorkerHandle struct</name>
  <files>src/worker/tenant_pool.rs</files>
  <read_first>
    - src/worker/tenant_pool.rs (lines 27-60 — imports, TenantPool struct, TenantWorker struct)
    - src/worker/tenant_pool.rs (lines 61-114 — TenantPool::new and spawn_worker)
    - src/config/app.rs (effective_max_ws_connections and effective_ws_idle_timeout_ms methods — added by Plan 01)
  </read_first>
  <action>
    1. Add imports at top of file (near line 27): `use std::sync::atomic::AtomicUsize;` and `use std::sync::Arc;` and `use std::sync::Mutex;`

    2. Define WsWorkerHandle struct BELOW TenantWorker (around line 59). Fields:
       - task_tx: mpsc::Sender&lt;HandlerTask&gt; (same type as TenantWorker.task_tx — reuses existing channel type)
       - join: Option&lt;thread::JoinHandle&lt;()&gt;&gt;

    3. Add four fields to TenantPool struct (after control_plane field):
       - ws_workers: Mutex&lt;Vec&lt;WsWorkerHandle&gt;&gt; — lazy, starts empty. Mutex because dispatch_ws needs mutable access from async context while TenantPool is behind Arc.
       - ws_busy: Arc&lt;AtomicUsize&gt; — shared with worker threads so they can decrement on connection close. Initialize to 0.
       - max_ws_connections: u32 — populated from AppLimits::effective_max_ws_connections() in TenantPool::new().
       - ws_idle_timeout_ms: u64 — populated from AppLimits::effective_ws_idle_timeout_ms() in TenantPool::new().

    4. Update TenantPool::new() signature to accept an additional parameter: limits: &amp;crate::config::app::AppLimits. Initialize the four new fields: ws_workers: Mutex::new(Vec::new()), ws_busy: Arc::new(AtomicUsize::new(0)), max_ws_connections: limits.effective_max_ws_connections(), ws_idle_timeout_ms: limits.effective_ws_idle_timeout_ms(). Log effective WS config in the existing info! message.

    5. Update ALL callers of TenantPool::new() to pass the limits parameter. Search for `TenantPool::new(` across the codebase. The caller likely has access to AppLimits through AppConfig or similar. If the caller does not have AppLimits, use AppLimits::default() as fallback (yields floor(4/2)=2 connections, 30s timeout).

    6. Update run_worker function signature in tenant_pool.rs to accept `ws_busy: Arc<AtomicUsize>` as a new parameter (after existing parameters). In the body, add `let _ = &ws_busy;` as a placeholder — Plan 04 will use it. Update all existing spawn_worker / run_worker call sites to pass `Arc::clone(&ws_busy_arc)`.

    7. Implement pub fn dispatch_ws(&amp;self, task: HandlerTask) -&gt; Result&lt;()&gt; on TenantPool:
       - Load ws_busy with Ordering::SeqCst. If >= max_ws_connections, return Err(anyhow!("WebSocket connection limit reached")).
       - Lock ws_workers Mutex. Search for an idle worker: iterate ws_workers looking for one whose task_tx.send() would not block (try sending the task). If a worker's task_tx has been disconnected (receiver dropped = thread exited), remove that handle from the Vec.
       - If no idle worker found: spawn a new WS worker thread via run_worker, passing Arc::clone(&self.ws_busy). Push the new WsWorkerHandle into ws_workers. Send the task via the new worker's task_tx.
       - Note: ws_busy is incremented by the WORKER thread when it receives a WS task (inside run_worker), not here. This avoids TOCTOU between the check and the send per D-13b.

    7. Extend the Drop impl for TenantPool (or add one if missing). After joining existing HTTP workers, lock ws_workers and join all WS worker thread handles. This prevents V8 isolate use-after-platform-shutdown per Pitfall 7 in RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/rust/nano-rs && cargo check --lib 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TenantPool struct has fields: ws_workers (Mutex Vec), ws_busy (Arc AtomicUsize), max_ws_connections (u32), ws_idle_timeout_ms (u64)
    - WsWorkerHandle struct exists with task_tx and join fields
    - TenantPool::new() accepts AppLimits parameter and initializes WS fields
    - dispatch_ws() method exists, checks ws_busy against max_ws_connections, spawns lazy workers
    - Drop impl joins WS worker threads
    - cargo check --lib passes with 0 errors
    - grep -c "dispatch_ws" src/worker/tenant_pool.rs returns at least 1
    - grep -c "ws_busy" src/worker/tenant_pool.rs returns at least 3
  </acceptance_criteria>
  <done>TenantPool extended with lazy WS worker pool, dispatch_ws method enforces connection limit per D-07, Drop joins WS threads per Pitfall 7</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP caller -> TenantPool::dispatch_ws | Untrusted external client triggers WS connection attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | DoS | TenantPool::dispatch_ws | mitigate | ws_busy counter checked against max_ws_connections before spawning; returns error at limit per D-07 |
| T-23-03 | DoS | ws_workers Vec unbounded growth | mitigate | Dead worker handles pruned on each dispatch_ws call; idle workers exit via recv_timeout |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new crates — only using existing std::sync types |
</threat_model>

<verification>
1. `cargo check --lib` passes
2. `grep -c "dispatch_ws" src/worker/tenant_pool.rs` >= 1
3. `grep -c "ws_busy" src/worker/tenant_pool.rs` >= 3
4. `grep -c "WsWorkerHandle" src/worker/tenant_pool.rs` >= 2
5. `grep -c "max_ws_connections" src/worker/tenant_pool.rs` >= 2
</verification>

<success_criteria>
- TenantPool can dispatch WebSocket tasks to lazy worker pool
- Connection limit enforced per D-07 (max_ws_connections)
- Dead WS worker handles cleaned up on dispatch
- WS worker threads joined on TenantPool Drop
- All existing tests pass (no regressions from new() signature change)
</success_criteria>

<output>
Create `.planning/phases/23-websocket-server/23-02-SUMMARY.md` when done
</output>
