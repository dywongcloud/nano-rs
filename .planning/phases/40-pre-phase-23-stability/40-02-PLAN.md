---
phase: "40"
plan: "02"
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - tests/isolate_endurance_test.rs
autonomous: true
requirements:
  - STAB-02
  - STAB-03
  - STAB-04
must_haves:
  truths:
    - "isolate_endurance_test.rs exists with ENDURE-01 (exception recovery), ENDURE-02 (state isolation doc), ENDURE-03 (10+ requests) test cases"
    - "All three endurance tests pass with cargo test --test isolate_endurance_test"
    - "Tests use a 1-worker pool so request->worker affinity is deterministic"
    - "Exception-throwing requests return Err or status 500; the immediately following request succeeds with status 200"
  artifacts:
    - path: "tests/isolate_endurance_test.rs"
      provides: "ENDURE-01, ENDURE-02, ENDURE-03 test functions"
      contains: "ENDURE-01"
  key_links:
    - from: "tests/isolate_endurance_test.rs"
      to: "nano::worker::pool::WorkerPool"
      via: "WorkerPool::with_backend(hostname, 1, 0, backend)"
      pattern: "with_backend.*1.*0"
---

<objective>
Create tests/isolate_endurance_test.rs with three focused test cases that prove the
correctness properties from STAB-02, STAB-03, and STAB-04.

Purpose: The existing isolate_scope_test.rs proves the persistent-scope mechanism works
in isolation (direct V8 calls). These endurance tests prove it works end-to-end through
the WorkerPool dispatch path, including exception recovery across sequential requests.

Output: tests/isolate_endurance_test.rs (new file, 3 test functions) passing with
cargo test --test isolate_endurance_test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-pre-phase-23-stability/40-RESEARCH.md
@.planning/phases/40-pre-phase-23-stability/40-01-SUMMARY.md

<interfaces>
<!-- From tests/isolate_scope_test.rs (existing, confirmed passing): -->

Helper functions to copy into isolate_endurance_test.rs:

  fn init_v8() { let _ = nano::v8::initialize_platform(); }

  fn make_backend() -> nano::vfs::VfsBackendEnum {
      nano::vfs::VfsBackendEnum::Memory(std::sync::Arc::new(
          nano::vfs::MemoryBackend::default()
      ))
  }

  fn write_js(name: &str, code: &str) -> String {
      let dir = std::env::temp_dir().join("nano_endurance_test");
      std::fs::create_dir_all(&dir).unwrap();
      let path = dir.join(name);
      std::fs::write(&path, code).unwrap();
      path.to_str().unwrap().to_string()
  }

  fn make_get(url: &str) -> nano::http::NanoRequest {
      nano::http::NanoRequest::new(
          "GET".to_string(),
          nano::http::NanoUrl::parse(url).unwrap(),
          nano::http::NanoHeaders::new(),
          None,
      )
  }

WorkerPool construction (1-worker pool for deterministic affinity):
  let pool = WorkerPool::with_backend(
      "endurance.test".into(),
      1,   // EXACTLY 1 worker -- all requests hit the same persistent scope
      0,   // no memory limit
      make_backend(),
  );

HandlerTask construction:
  let (tx, rx) = std::sync::mpsc::channel();
  let task = nano::worker::HandlerTask {
      entrypoint: entrypoint.clone(),
      request: make_get("http://endurance.test/"),
      request_id: format!("req-{}", i),
      cpu_time_limit_ms: 0,
      response_tx: tx,
  };
  pool.workers[0].send(task).unwrap();   // or pool.dispatch(task)
  let result = rx.recv().unwrap();       // blocks until worker responds

NOTE: Check the actual HandlerTask field names by reading src/worker/mod.rs or
src/worker/handler_task.rs before writing the test. The fields shown above are
approximate; confirm the actual struct layout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read HandlerTask struct and write ENDURE-01 (exception recovery test)</name>
  <files>tests/isolate_endurance_test.rs</files>
  <read_first>
    - src/worker/mod.rs or wherever HandlerTask is defined -- grep for "pub struct HandlerTask" to find exact field names and types
    - tests/isolate_scope_test.rs lines 1-100 -- copy the exact import pattern and helper functions
    - src/worker/pool.rs lines 550-620 -- find the dispatch() method signature to know if pool.dispatch(task) or pool.workers[id].send(task) is the correct API for a known worker
  </read_first>
  <behavior>
    - ENDURE-01: Send 30 requests to a 1-worker pool where the JS handler throws on every 3rd request (call_count % 3 == 0). After each throw, the NEXT request (call_count % 3 != 0) must return status 200, not Err or status 500.
    - Exception-throwing requests must NOT cause a panic -- they should return Ok(500) or Err(_); both are acceptable outcomes for the throw request itself.
    - Request immediately following a throw must return Ok(200) -- this is the critical assertion.
  </behavior>
  <action>
    Read src/worker/mod.rs (or grep "pub struct HandlerTask") first to confirm field names.
    Read tests/isolate_scope_test.rs lines 1-100 to copy the import style.

    Create tests/isolate_endurance_test.rs. The file structure:
    1. Imports block matching isolate_scope_test.rs style
    2. Helper functions (init_v8, make_backend, write_js, make_get) -- copy verbatim from scope test, changing the temp dir name to "nano_endurance_test"
    3. fn endure_01_exception_recovery() test function marked #[test]:
       - Calls init_v8()
       - Writes a JS handler to a temp file that uses a module-level call_count variable, increments on each call, throws when call_count % 3 == 0, returns { status: 200, body: "ok:" + call_count, headers: {} } otherwise
       - Creates a 1-worker WorkerPool using WorkerPool::with_backend("endurance.test".into(), 1, 0, make_backend())
       - Sends 30 requests sequentially via the pool's dispatch method or direct worker send
       - For each request where (i+1) % 3 == 0: asserts result is Ok(500) or Err -- both acceptable
       - For each request where (i+1) % 3 != 0 AND i > 0: asserts result is Ok(200)
       - Key assertion: request at index 3 (first non-throw after a throw) must be Ok(200)
    4. DO NOT write ENDURE-02 and ENDURE-03 yet -- those go in Task 2.

    JS handler template (write as a string literal in Rust, no fenced blocks here):
      var call_count = 0;
      function __nano_user_fetch(req) {
        call_count++;
        if (call_count % 3 === 0) { throw new Error("intentional at " + call_count); }
        return { status: 200, body: "ok:" + call_count, headers: {} };
      }

    Run: cargo test --test isolate_endurance_test endure_01 2>&1 | tail -10
  </action>
  <verify>
    <automated>cargo test --test isolate_endurance_test endure_01 2>&1 | grep -E "^test result|FAILED|ok$"</automated>
  </verify>
  <done>
    cargo test --test isolate_endurance_test endure_01 shows: test endure_01_exception_recovery ... ok
    No FAILED lines in output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write ENDURE-02 (state isolation doc) and ENDURE-03 (10+ requests endurance)</name>
  <files>tests/isolate_endurance_test.rs</files>
  <read_first>
    - tests/isolate_endurance_test.rs -- read the full current file to understand what is already present before adding ENDURE-02 and ENDURE-03
  </read_first>
  <behavior>
    - ENDURE-02: Send 5 requests to a 1-worker pool. The JS handler increments a module-level counter and returns it. Assert counter values are 1, 2, 3, 4, 5 in order (monotonically increasing -- module-global state persists within one isolate lifetime). This documents CF-Workers semantics: NOT a bug.
    - ENDURE-03: Send 15 requests to a 1-worker pool. All must return Ok(200). No None returns, no panics, no Err. Verifies 10+ requests per worker pass without degradation (STAB-04).
  </behavior>
  <action>
    Append to tests/isolate_endurance_test.rs:

    ENDURE-02 test function (fn endure_02_module_state_persists):
    - 1-worker pool
    - JS handler: var request_count = 0; function __nano_user_fetch(req) { request_count++; return { status: 200, body: String(request_count), headers: {} }; }
    - Send 5 sequential requests
    - Parse body as integer for each response; assert values are [1, 2, 3, 4, 5]
    - Add a comment above the test: "// [ENDURE-02] Module-level state persists within one isolate lifetime -- CF-Workers semantics. This is correct behaviour, NOT a bug."

    ENDURE-03 test function (fn endure_03_ten_plus_requests_no_degradation):
    - 1-worker pool
    - JS handler: function __nano_user_fetch(req) { return { status: 200, body: "ok", headers: {} }; }
    - Send 15 sequential requests
    - Assert every response is Ok(200) -- use assert_eq!(result.unwrap().status(), 200, "Request {} degraded", i)

    Run all three tests:
    cargo test --test isolate_endurance_test 2>&1 | tail -10
  </action>
  <verify>
    <automated>cargo test --test isolate_endurance_test 2>&1 | grep -E "^test result|FAILED"</automated>
  </verify>
  <done>
    cargo test --test isolate_endurance_test shows "test result: ok. 3 passed; 0 failed".
    The three test names visible in output:
      test endure_01_exception_recovery ... ok
      test endure_02_module_state_persists ... ok
      test endure_03_ten_plus_requests_no_degradation ... ok
    git add + git commit: "test(endurance): ENDURE-01..03 -- exception recovery, state isolation, 10+ request endurance"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test JS handlers -> V8 | Test-controlled JS that intentionally throws to exercise recovery path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05 | Denial of Service | Exception from test handler poisoning isolate | mitigate | ENDURE-01 verifies TryCatch clears exception; this IS the test of the mitigation |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies |
</threat_model>

<verification>
cargo test --test isolate_endurance_test 2>&1 | grep "^test result"   # 3 passed; 0 failed
cargo test --test isolate_scope_test 2>&1 | grep "^test result"       # 9 passed; 0 failed (regression check)
</verification>

<success_criteria>
tests/isolate_endurance_test.rs exists with ENDURE-01, ENDURE-02, ENDURE-03 tests.
All three pass. SCOPE tests still pass. Requirements STAB-02, STAB-03, STAB-04 covered.
</success_criteria>

<output>
Create `.planning/phases/40-pre-phase-23-stability/40-02-SUMMARY.md` when done.
</output>
