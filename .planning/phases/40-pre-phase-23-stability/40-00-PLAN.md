---
phase: "40"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/worker/pool.rs
  - src/worker/tenant_pool.rs
  - src/runtime/apis.rs
autonomous: true
requirements:
  - STAB-01
  - STAB-02
must_haves:
  truths:
    - "All in-session fixes (TryCatch, eval-blocking, Buffer.from) are in version control"
    - "cargo build --lib compiles with 0 errors and 0 warnings"
    - "SCOPE-01..08 still pass after committing the session fixes"
  artifacts:
    - path: "src/worker/pool.rs"
      provides: "TryCatch at both handler_local.call() sites + set_allow_generation_from_strings(false)"
      contains: "TryCatch::new"
    - path: "src/worker/tenant_pool.rs"
      provides: "TryCatch at handler_local.call() site + set_allow_generation_from_strings(false)"
      contains: "TryCatch::new"
    - path: "src/runtime/apis.rs"
      provides: "Buffer.from(Array) fix -- is_array() guard before to_string() coercion"
      contains: "is_array"
  key_links:
    - from: "src/worker/pool.rs"
      to: "v8::TryCatch"
      via: "pin-and-init pattern (TryCatch::new + std::pin::pin! + .init())"
      pattern: "TryCatch::new\\(&mut \\*ctx_scope\\)"
---

<objective>
Commit the three in-session fixes applied on 2026-05-17 but not committed: TryCatch
wrapping at all handler call sites, string-generation blocking, and Buffer.from(Array)
type guard.

Purpose: These fixes must be in version control before Wave 1 investigation begins. They
form the correctness baseline. Without them the subsequent waves cannot distinguish between
regressions from the uncommitted changes and the root cause being investigated.

Output: Git commit containing src/worker/pool.rs (TryCatch sites), src/worker/tenant_pool.rs
(TryCatch site), and src/runtime/apis.rs (Buffer.from fix).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-pre-phase-23-stability/40-RESEARCH.md
@.planning/STATE.md

<interfaces>
<!-- V8 TryCatch pin-and-init pattern (v8 crate 147.4.0) -->
<!-- TryCatch::new returns ScopeStorage, which must be pinned before calling .init() -->

TryCatch creation (REQUIRED pattern):
  let tc_storage = v8::TryCatch::new(&mut *ctx_scope);
  let tc_pin = std::pin::pin!(tc_storage);
  let mut tc = tc_pin.init();
  // All V8 ops take &tc (shared ref), NOT &mut tc
  let call_result = handler_local.call(&tc, global_obj.into(), &[js_req.into()]);

String generation blocking (called on Context BEFORE ContextScope::new):
  context.set_allow_generation_from_strings(false);
  // In pool.rs this is around line 256; confirm with read_first

Buffer.from guard in apis.rs:
  // Before: coerces Array to string via to_string() -- wrong
  // After: checks is_array() first; if true, iterate elements as u8; else to_string()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and stage pool.rs and tenant_pool.rs session fixes</name>
  <files>src/worker/pool.rs, src/worker/tenant_pool.rs</files>
  <read_first>
    - src/worker/pool.rs -- confirm TryCatch::new present in the request-handling closure (~line 429); confirm set_allow_generation_from_strings(false) before ContextScope::new (~line 256)
    - src/worker/tenant_pool.rs -- confirm TryCatch::new present at the handler_local.call() site (~line 329); confirm set_allow_generation_from_strings(false) at its Context::new() site (~line 184)
    - Run: git diff src/worker/pool.rs src/worker/tenant_pool.rs
  </read_first>
  <action>
    Inspect the git diff output for pool.rs and tenant_pool.rs. Verify these three
    assertions hold for each file:

    1. pool.rs contains v8::TryCatch::new at least once inside the request-handling closure
    2. pool.rs contains context.set_allow_generation_from_strings(false) before ContextScope::new
    3. tenant_pool.rs contains both of the above at its corresponding sites

    If any assertion fails, apply the missing fix now using the exact pin-and-init pattern
    shown in the interfaces block. One TryCatch per closure body is correct -- do not add
    a second TryCatch inside the same closure if one already covers the call site.

    Once both files are verified correct, stage them:
      git add src/worker/pool.rs src/worker/tenant_pool.rs

    Do NOT commit yet -- Task 2 adds apis.rs to the same commit.
  </action>
  <verify>
    <automated>grep -c "TryCatch::new" /Users/gleicon/code/rust/nano-rs/src/worker/pool.rs && grep -c "TryCatch::new" /Users/gleicon/code/rust/nano-rs/src/worker/tenant_pool.rs && grep -c "set_allow_generation_from_strings" /Users/gleicon/code/rust/nano-rs/src/worker/pool.rs</automated>
  </verify>
  <done>pool.rs grep count for TryCatch::new >= 1; tenant_pool.rs grep count for TryCatch::new >= 1; pool.rs grep count for set_allow_generation_from_strings >= 1; git diff --cached lists both files staged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify apis.rs Buffer.from fix and commit all session fixes</name>
  <files>src/runtime/apis.rs</files>
  <read_first>
    - src/runtime/apis.rs -- find the Buffer.from binding function; look for is_array() guard on the first argument before any to_string() coercion
    - Run: git diff src/runtime/apis.rs
  </read_first>
  <action>
    Run git diff src/runtime/apis.rs to confirm the Buffer.from(Array) fix is present.
    The fix must check arg.is_array() before coercing with to_string(). If the array
    guard is missing, add it: when the first argument is_array(), iterate its indices
    as integer values (u8 bytes); otherwise fall through to the existing to_string() path.

    Once confirmed, stage apis.rs:
      git add src/runtime/apis.rs

    Then commit all three staged files:
      git commit -m "fix(worker): TryCatch at handler call sites, block string generation, fix Buffer.from(Array)"

    After commit, run the isolate scope tests to confirm the committed code still passes:
      cargo test --test isolate_scope_test 2>&1 | tail -5
  </action>
  <verify>
    <automated>cargo test --test isolate_scope_test 2>&1 | grep -E "^test result|FAILED|passed"</automated>
  </verify>
  <done>
    git log --oneline -1 shows the commit with all three files.
    cargo test --test isolate_scope_test shows 0 FAILED (all 9 tests pass).
    cargo build --lib 2>&1 | grep -c "^error" outputs 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS handler -> V8 isolate | User-supplied JS code runs inside the isolate |
| String evaluation | Dynamic code execution via string-based APIs poses injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | String-based code generation (eval-equivalent) | mitigate | context.set_allow_generation_from_strings(false) at all Context::new() sites -- session fix being committed |
| T-40-02 | Denial of Service | Exception state leaking across requests | mitigate | TryCatch RAII at handler call sites -- exception cleared on drop |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this plan |
</threat_model>

<verification>
cargo build --lib 2>&1 | grep -c "^error"   # must output 0
cargo test --test isolate_scope_test         # 9 tests must pass
git log --oneline -1                         # shows the session-fix commit
</verification>

<success_criteria>
All three in-session fixes committed to main branch. cargo build --lib passes with 0 errors.
SCOPE-01..08 (9 tests in isolate_scope_test.rs) all pass.
</success_criteria>

<output>
Create `.planning/phases/40-pre-phase-23-stability/40-00-SUMMARY.md` when done.
</output>
