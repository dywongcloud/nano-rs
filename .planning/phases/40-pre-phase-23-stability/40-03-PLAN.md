---
phase: "40"
plan: "03"
type: execute
wave: 3
depends_on:
  - "40-02"
files_modified:
  - /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js
autonomous: true
requirements:
  - STAB-03
must_haves:
  truths:
    - "strict-multi-request-tests.js runs against fixed server (Wave 1 fix applied) and all 12 tests pass or failures are documented as known state-semantics differences"
    - "No test in strict-multi-request-tests.js asserts counter === 1 on request 5+ when CF-Workers persistent-scope means counter will be higher (worker reused)"
    - "Test suite still runs without JS syntax errors after any changes"
  artifacts:
    - path: "/Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js"
      provides: "Multi-request tests updated to match CF-Workers semantics, run against Wave 1 fixed server"
      contains: "CF-Workers"
  key_links:
    - from: "/Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js"
      to: "nano-rs WorkerPool (fixed with cancel_terminate_execution)"
      via: "HTTP requests to running nano-rs server"
      pattern: "strict-multi-request"
---

<objective>
Run the external test suite against the Wave 1-fixed server and fix any wrong
invariants — static counter assumptions that fail under CF-Workers persistent-scope
semantics even when the server is working correctly.

Context from test report (2026-05-17): Active test files are:
  - scripts/strict-multi-request-tests.js  (12 tests, multi-request per server — finds real bugs)
  - scripts/v15-isolate-aware-tests.js  (22 tests, fresh server per batch — masks bugs via restart)

After Wave 1 fix (cancel_terminate_execution), strict-multi-request-tests.js should pass
for all stateless handlers. Stateful counter tests may still "fail" if they expect counter=1
on request 5 (which goes to recycled worker 0 with counter already incremented from request 1).
These are test design issues, not server bugs.

Bug 3 from test report:
  Request 5 expected: counter=1, actual: counter=2
  Root: 4-worker pool round-robin, worker 0 ran request 1 (counter→1) AND request 5 (counter→2)
  With cancel_terminate_execution fix: request 5 no longer returns 500 — it returns 200
  with counter=2. If the test expects counter=1, the TEST is wrong (not the server).

Output: strict-multi-request-tests.js with updated counter-state assertions that match
CF-Workers semantics, committed to the test suite repo. 12/12 tests expected to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-pre-phase-23-stability/40-RESEARCH.md
@.planning/phases/40-pre-phase-23-stability/40-02-SUMMARY.md

<interfaces>
<!-- Test suite location: /Users/gleicon/code/js/nano-rs-test-suite/scripts/ -->
<!-- Active files (per test report 2026-05-17): -->
<!--   strict-multi-request-tests.js  (12 tests, multi-request per server) -->
<!--   v15-isolate-aware-tests.js  (22 tests, fresh server per batch) -->
<!--   test-utils.js  (NanoServer class, shared utilities) -->

<!-- Counter state semantics (confirmed from test report Bug 3): -->
<!--   4-worker pool, round-robin dispatch -->
<!--   Request 1 → worker 0 (counter: 0→1) -->
<!--   Request 2 → worker 1 (counter: 0→1) -->
<!--   Request 3 → worker 2 (counter: 0→1) -->
<!--   Request 4 → worker 3 (counter: 0→1) -->
<!--   Request 5 → worker 0 (counter: 1→2)  ← "2" is CORRECT CF-Workers behaviour -->
<!--   Test that expects counter=1 on request 5 is WRONG. Fix: expect counter=2, or use 1-worker pool. -->

<!-- Endurance test failure mode (per test report §9.3): -->
<!--   Before Wave 1 fix: request 5 → HTTP 500 (terminate flag poisoned worker 0) -->
<!--   After Wave 1 fix: request 5 → HTTP 200, body="2" (correct) -->

<!-- Key file to read before editing: -->
<!--   scripts/strict-multi-request-tests.js -- all 12 tests, identify which check counter values -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run strict-multi-request-tests.js against Wave 1-fixed server and audit failures</name>
  <files>/Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js</files>
  <read_first>
    - /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js -- read ALL 12 tests; identify which ones check counter or id values as literal integers
    - /Users/gleicon/code/js/nano-rs-test-suite/scripts/test-utils.js -- read NanoServer class to understand how to start server for test run
  </read_first>
  <action>
    Read strict-multi-request-tests.js in full. For each test, classify as:
    A) STATELESS — returns computed/fixed value, not affected by counter accumulation → should pass after Wave 1 fix
    B) STATEFUL (correct) — verifies that module state persists within a worker (e.g., counter=2 on request 5) → should pass after Wave 1 fix
    C) STATEFUL (wrong invariant) — expects counter=1 on request 5 or expects fresh state on recycled workers → WRONG; needs fix

    Start a nano-rs server with the Wave 1-fixed binary:
      cd /tmp && nano-rs run --config [minimal config] &
      sleep 2
      node /Users/gleicon/code/js/nano-rs-test-suite/scripts/run-all-tests.js 2>&1 | tee /tmp/test-run.txt

    Review /tmp/test-run.txt. For each FAILED test: determine if it fails due to:
    (a) a server bug not fixed by Wave 1 → document as known issue
    (b) a wrong invariant in the test → mark as "needs fix in Task 2"

    Document all (b) cases with the exact line numbers to fix.
  </action>
  <verify>
    <automated>ls /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js && echo "file exists"</automated>
  </verify>
  <done>
    Audit complete: list of tests classified A/B/C. Category C tests have exact line numbers identified for Task 2.
    If zero Category C tests found: document "No wrong invariants found" and skip Task 2 edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix wrong counter invariants in strict-multi-request-tests.js</name>
  <files>/Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js</files>
  <read_first>
    - /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js -- read the specific lines identified by Task 1 as Category C (wrong invariant)
  </read_first>
  <action>
    For each Category C test identified by Task 1:

    Fix pattern for "counter === 1 on request 5" type assertions:
    - Replace hard-coded expected value 1 with a dynamic check
    - For a 4-worker pool: request 5 hits worker 0 again, counter will be 2 (correct)
    - Correct assertion: "counter >= 1" (counter is positive integer, exact value depends on pool routing)
    - OR: change to use 1-worker pool (set workers: 1 in config) so counter is deterministic

    Add a comment above each fixed assertion:
    // CF-Workers semantics: with N-worker pool and round-robin routing, request M hits
    // worker (M-1) % N. Module-level counter persists within that worker's isolate lifetime.
    // After Wave 1 fix (cancel_terminate_execution), recycled workers are healthy — counter
    // increments correctly from prior value.

    After editing, verify the file is syntactically valid:
    node --check /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js

    Re-run tests against fixed server:
    node /Users/gleicon/code/js/nano-rs-test-suite/scripts/run-all-tests.js 2>&1 | tail -20

    Commit in the test suite repo:
    cd /Users/gleicon/code/js/nano-rs-test-suite
    git add scripts/strict-multi-request-tests.js
    git commit -m "fix(tests): update counter assertions to match CF-Workers persistent-scope semantics"
  </action>
  <verify>
    <automated>node --check /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js && echo "syntax ok"</automated>
  </verify>
  <done>
    node --check exits 0 (syntax valid).
    Test run shows 12/12 tests pass (or all failures are documented server-side bugs, not test design issues).
    git log --oneline -1 in nano-rs-test-suite shows the fix commit.
    grep "CF-Workers semantics" in strict-multi-request-tests.js shows the new comments.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite -> running nano-rs server | External HTTP calls to nano-rs; test suite not in the same repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Information Disclosure | Test suite mis-reporting correct behaviour as a bug | mitigate | Update warn message to accurately describe CF-Workers state semantics |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies |
</threat_model>

<verification>
node --check /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js && echo "syntax ok"
grep -c "CF-Workers semantics" /Users/gleicon/code/js/nano-rs-test-suite/scripts/strict-multi-request-tests.js
node /Users/gleicon/code/js/nano-rs-test-suite/scripts/run-all-tests.js 2>&1 | tail -5
</verification>

<success_criteria>
strict-multi-request-tests.js is syntactically valid. Tests updated to match CF-Workers
persistent-scope semantics. 12/12 tests pass against Wave 1-fixed server (or all failures
are documented as known server-side issues). Requirement STAB-03 covered.
</success_criteria>

<output>
Create `.planning/phases/40-pre-phase-23-stability/40-03-SUMMARY.md` when done.
</output>
