---
phase: 01-v8-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/lib.rs
autonomous: true
requirements:
  - FND-01
user_setup: []
must_haves:
  truths:
    - "cargo build produces a binary without V8 compilation"
    - "Project structure follows single-crate modular pattern"
    - "Dependencies include v8, tokio, tracing as specified in D-06"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project configuration with rusty_v8 dependency"
      contains: "v8 = \"147\""
    - path: "src/main.rs"
      provides: "Binary entry point"
      min_lines: 20
    - path: "src/lib.rs"
      provides: "Library exports for testing"
      min_lines: 15
    - path: "src/v8/mod.rs"
      provides: "V8 module skeleton"
      min_lines: 10
    - path: "src/runtime/mod.rs"
      provides: "Runtime module placeholder"
      min_lines: 5
    - path: "src/http/mod.rs"
      provides: "HTTP module placeholder"
      min_lines: 5
  key_links:
    - from: "src/main.rs"
      to: "src/lib.rs"
      via: "mod lib or use nano::"
      pattern: "use nano|mod lib|pub mod"
---

<objective>
Create Rust project skeleton with cargo configuration and modular structure per D-01. This is the foundation all subsequent plans build upon.

Purpose: Establish the project structure and dependencies required for V8 integration.
Output: Working cargo project with rusty_v8 dependency configured for pre-built binaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-v8-foundation/01-CONTEXT.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo.toml with dependencies</name>
  <files>Cargo.toml</files>
  <action>
Create Cargo.toml with the following structure per D-01 and D-06:

1. Package metadata for nano-rs
2. Dependencies section with EXACT versions from research:
   - v8 = "147" (rusty_v8, pre-built)
   - tokio = { version = "1.52", features = ["full"] }
   - tracing = "0.1"
   - anyhow = "1.0"
   - thiserror = "2.0"

3. IMPORTANT: Do NOT include deno_core - use rusty_v8 directly per research decision
4. Set edition = "2021"
5. Add [profile.release] with opt-level = 3

Reference STACK.md §Dependencies to Add for full list.
  </action>
  <verify>
    <automated>cargo check --message-format=short 2>&1 | head -20</automated>
  </verify>
  <done>Cargo.toml exists with v8 = "147", tokio, tracing dependencies; cargo check passes without V8 compilation (pre-built binary downloaded)</done>
</task>

<task type="auto">
  <name>Task 2: Create modular source structure</name>
  <files>
    src/main.rs
    src/lib.rs
    src/v8/mod.rs
    src/runtime/mod.rs
    src/http/mod.rs
  </files>
  <action>
Create the module structure per D-01 specifics section:

1. src/main.rs - Binary entry point:
   - fn main() with anyhow::Result return type
   - Basic tracing subscriber initialization
   - Placeholder call to lib::run()

2. src/lib.rs - Library root:
   - pub mod v8
   - pub mod runtime
   - pub mod http
   - pub fn run() -> anyhow::Result<()> placeholder

3. src/v8/mod.rs - V8 module skeleton:
   - Module documentation: "V8 integration with EPT fix"
   - Placeholder functions for platform init, isolate creation

4. src/runtime/mod.rs - Runtime module (placeholder for Phase 3):
   - Module documentation: "JavaScript runtime APIs (Phase 3)"

5. src/http/mod.rs - HTTP module (placeholder for Phase 2):
   - Module documentation: "HTTP server (Phase 2)"

All modules should compile without warnings.
  </action>
  <verify>
    <automated>cargo build 2>&1 | grep -E "(Compiling|Finished|error)" | head -10</automated>
  </verify>
  <done>All 5 source files exist; cargo build succeeds; no compilation errors; module hierarchy established</done>
</task>

<task type="auto">
  <name>Task 3: Verify pre-built V8 download works</name>
  <files>target/debug/nano-rs</files>
  <action>
Verify the build system downloads pre-built V8 binaries instead of compiling from source:

1. Run cargo clean to clear any cached artifacts
2. Run cargo build and monitor output
3. Verify output shows downloading from RUSTY_V8_MIRROR, NOT "Compiling V8"
4. Check that build completes in under 5 minutes (pre-built) vs 30-120 minutes (from source)
5. Run the binary: ./target/debug/nano-rs should output startup message

If V8 is being compiled from source, check that V8_FROM_SOURCE is NOT set in environment.
  </action>
  <verify>
    <automated>cargo clean && cargo build 2>&1 | tee /tmp/build.log && echo "---" && grep -E "(Downloading|rusty_v8|Compiling v8|Finished)" /tmp/build.log | head -20</automated>
  </verify>
  <done>Build log shows downloading pre-built rusty_v8; binary exists at target/debug/nano-rs; runs without error</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build system → V8 binaries | Downloading pre-built V8 from external source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | V8 binary download | mitigate | Verify via Cargo.lock checksums; official denoland/rusty_v8 releases |
| T-01-02 | Denial of Service | Build time | accept | Pre-built binaries prevent 2-hour compile; acceptable risk |
</threat_model>

<verification>
1. `cargo build` completes without errors
2. Binary exists and runs
3. Build log confirms pre-built V8 was downloaded, not compiled
4. All modules (v8, runtime, http) are importable
</verification>

<success_criteria>
- Project compiles successfully with `cargo build`
- Pre-built V8 binaries are downloaded (not compiled from source)
- Modular structure exists: main.rs, lib.rs, v8/, runtime/, http/
- Binary runs and produces output
</success_criteria>

<output>
After completion, create `.planning/phases/01-v8-foundation/01-01-SUMMARY.md`
</output>
